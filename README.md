# 🐘 PHP com Docker

Exemplo de projeto usando **PHP** dentro de um **container Docker**.  
O script PHP simples retorna uma mensagem de teste em HTML.

---

## 🚀 Tecnologias utilizadas

- PHP  
- Docker  
- Docker Compose  

---

## 📦 Como clonar o projeto

```bash
# Clonar o repositório
git clone https://github.com/AndersonMeloo/php-docker.git

# Acessar a pasta do projeto
cd php-docker
```

## ⚙️ Rodando com Docker

```bash
# Buildar e iniciar o container
docker compose up --build
```
